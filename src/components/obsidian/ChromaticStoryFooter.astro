<footer class="story-footer" aria-label="Site directory and footer">
  <div class="story-footer__grid">
    <div class="story-footer__card story-footer__card--work">
      <div class="story-footer__card-header">
        <h3 class="story-footer__title">Work</h3>
        <span class="story-footer__index">01</span>
      </div>
      <nav aria-label="Work links">
        <a href="/work">All Projects <span aria-hidden="true">↗</span></a>
        <a href="/research">Research <span aria-hidden="true">↗</span></a>
        <a href="/photography">Photography <span aria-hidden="true">↗</span></a>
        <a href="/films">Filmmaking <span aria-hidden="true">↗</span></a>
        <a href="/blog">Writing <span aria-hidden="true">↗</span></a>
      </nav>
    </div>

    <div class="story-footer__card story-footer__card--curation">
      <div class="story-footer__card-header">
        <h3 class="story-footer__title">Curation</h3>
        <span class="story-footer__index">02</span>
      </div>
      <nav aria-label="Curation links">
        <a href="/resources">Resource Hub <span aria-hidden="true">↗</span></a>
        <a href="/moodboard">Visual Moodboard <span aria-hidden="true">↗</span></a>
        <a href="/xr-showcase">XR Showcase <span aria-hidden="true">↗</span></a>
        <a href="/curated-videos">Must Watch Videos <span aria-hidden="true">↗</span></a>
        <a href="/research/obsidian-vault">Obsidian Vault <span aria-hidden="true">↗</span></a>
      </nav>
    </div>

    <div class="story-footer__card story-footer__card--workshops">
      <div class="story-footer__card-header">
        <h3 class="story-footer__title">Workshops</h3>
        <span class="story-footer__index">03</span>
      </div>
      <nav aria-label="Workshops links">
        <a href="/obsidian-tutoring">Obsidian Tutoring <span aria-hidden="true">↗</span></a>
        <a href="/premiere-pro-masterclass">Video Editing Masterclass <span aria-hidden="true">↗</span></a>
        <a href="/workshops">Photography <span aria-hidden="true">↗</span></a>
        <a href="/workshops/video-editing-storytelling-class-1">Filmmaking <span aria-hidden="true">↗</span></a>
        <a href="/workshops">Autoethnography <span aria-hidden="true">↗</span></a>
      </nav>
    </div>

    <div class="story-footer__card story-footer__card--about">
      <div class="story-footer__card-header">
        <h3 class="story-footer__title">About</h3>
        <span class="story-footer__index">04</span>
      </div>
      <nav aria-label="About links">
        <a href="/about">About <span aria-hidden="true">↗</span></a>
        <a href="/awards">Awards <span aria-hidden="true">↗</span></a>
        <a href="/press">Press Mentions <span aria-hidden="true">↗</span></a>
        <a href="/manifesto">Manifesto <span aria-hidden="true">↗</span></a>
        <a href="https://jwipqbjxpmgyevfzpjjx.supabase.co/storage/v1/object/public/misc/cv/Abodid%20Sahoo%20-%20Photography%20&%20AI%20-%20CV.pdf" target="_blank" rel="noopener noreferrer">Resume <span aria-hidden="true">↗</span></a>
      </nav>
    </div>
  </div>

  <div class="story-footer__bottom-card">
    <div class="story-footer__brand-group">
      <a class="story-footer__brand" href="/" aria-label="Abodid Sahoo homepage">abodid.com</a>
      <span class="story-footer__copyright">© {new Date().getFullYear()} Abodid Sahoo. All rights reserved.</span>
    </div>

    <div class="story-footer__social">
      <a href="https://www.instagram.com/abodid.sahoo" target="_blank" rel="noopener noreferrer">Instagram ↗</a>
      <a href="https://uk.linkedin.com/in/abodidsahoo" target="_blank" rel="noopener noreferrer">LinkedIn ↗</a>
      <a href="https://vimeo.com/abodidsahoo" target="_blank" rel="noopener noreferrer">Vimeo ↗</a>
      <a href="https://github.com/abodidsahoo" target="_blank" rel="noopener noreferrer">GitHub ↗</a>
    </div>

    <div class="story-footer__legal">
      <a href="/privacy">Privacy Policy</a>
      <span aria-hidden="true">·</span>
      <a href="/sitemap">Sitemap</a>
      <span aria-hidden="true">·</span>
      <a href="/architecture">Architecture</a>
    </div>
  </div>
</footer>

<style>
  .story-footer {
    --pop-blue: #2444ca;
    --pop-pink: #ff7eb5;
    --pop-yellow: #ffe44f;
    --pop-lime: #caff48;
    --pop-cream: #fff8e8;
    --pop-ink: #15130f;
    --home-next-gap: 8px;
    --home-next-border: 1px solid rgba(21, 19, 15, 0.78);
    --home-next-radius-panel: clamp(18px, 2vw, 28px);
    --home-next-radius-control: 14px;
    --home-next-motion: 180ms ease;
    padding: clamp(2.5rem, 5vw, 4.5rem) var(--home-next-gap) var(--home-next-gap);
    border-top: 0;
    background: var(--pop-pink);
    color: var(--pop-ink);
  }

  .story-footer a {
    color: inherit;
    text-decoration: none;
  }

  .story-footer__grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: var(--home-next-gap);
  }

  .story-footer__card {
    padding: clamp(1.4rem, 2.4vw, 2.2rem);
    display: flex;
    flex-direction: column;
    gap: 1.15rem;
    border: var(--home-next-border);
    border-radius: var(--home-next-radius-panel);
    transition: transform var(--home-next-motion), box-shadow var(--home-next-motion);
  }

  .story-footer__card--work { background: var(--pop-yellow); }

  .story-footer__card--curation {
    border-color: rgba(255, 248, 232, 0.45);
    background: var(--pop-blue);
    color: var(--pop-cream);
  }

  .story-footer__card--workshops { background: var(--pop-lime); }
  .story-footer__card--about { background: var(--pop-cream); }

  .story-footer__card:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 18px rgba(21, 19, 15, 0.08);
  }

  .story-footer__card--curation:hover {
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.18);
  }

  .story-footer__card-header {
    padding-bottom: 0.85rem;
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.75rem;
    border-bottom: 1.5px solid currentColor;
  }

  .story-footer__title,
  .story-footer__index {
    margin: 0;
    color: inherit;
    font: 750 clamp(1.3rem, 1.8vw, 1.85rem)/1 var(--font-display, sans-serif);
    letter-spacing: -0.035em;
    text-transform: uppercase;
  }

  .story-footer__index { opacity: 0.92; }

  .story-footer__card nav {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .story-footer__card nav a {
    padding: 0.52rem 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid rgba(21, 19, 15, 0.22);
    color: var(--pop-ink);
    font: 750 clamp(0.75rem, 0.88vw, 0.84rem)/1.35 var(--font-mono, monospace);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    transition: transform var(--home-next-motion), color var(--home-next-motion);
  }

  .story-footer__card--curation nav a {
    border-bottom-color: rgba(255, 248, 232, 0.28);
    color: var(--pop-cream);
  }

  .story-footer__card nav a:last-child { border-bottom: 0; }
  .story-footer__card nav a:hover { transform: translateX(4px); }
  .story-footer__card--curation nav a:hover { color: var(--pop-yellow); }

  .story-footer__card nav a span {
    opacity: 0.75;
    font-size: 0.88rem;
    transition: transform var(--home-next-motion), opacity var(--home-next-motion);
  }

  .story-footer__card nav a:hover span {
    opacity: 1;
    transform: translate(2px, -2px);
  }

  .story-footer__bottom-card {
    margin-top: var(--home-next-gap);
    padding: clamp(1rem, 1.8vw, 1.35rem) clamp(1.2rem, 2.5vw, 2.2rem);
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1.25rem;
    border: var(--home-next-border);
    border-radius: var(--home-next-radius-panel);
    background: var(--pop-cream);
  }

  .story-footer__brand-group,
  .story-footer__social,
  .story-footer__legal {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
  }

  .story-footer__brand-group { gap: 1.25rem; }
  .story-footer__social { gap: 0.5rem; }

  .story-footer__brand {
    min-height: 38px;
    padding: 0 0.9rem;
    display: inline-flex;
    align-items: center;
    border: 1px solid var(--pop-ink);
    border-radius: var(--home-next-radius-control);
    background: var(--pop-yellow);
    color: var(--pop-ink) !important;
    font: 750 0.74rem/1 var(--font-mono, monospace);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition: transform var(--home-next-motion), box-shadow var(--home-next-motion);
  }

  .story-footer__brand:hover {
    transform: translateY(-2px);
    box-shadow: 0 3px 0 var(--pop-ink);
  }

  .story-footer__copyright {
    color: rgba(21, 19, 15, 0.82);
    font: 600 0.72rem/1.35 var(--font-mono, monospace);
  }

  .story-footer__social a {
    min-height: 34px;
    padding: 0 0.75rem;
    display: inline-flex;
    align-items: center;
    border: 1px solid var(--pop-ink);
    border-radius: var(--home-next-radius-control);
    background: #fff;
    color: var(--pop-ink);
    font: 700 0.68rem/1 var(--font-mono, monospace);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    transition: background-color var(--home-next-motion), transform var(--home-next-motion);
  }

  .story-footer__social a:hover {
    transform: translateY(-2px);
    background: var(--pop-pink);
  }

  .story-footer__legal {
    gap: 0.85rem;
    color: rgba(21, 19, 15, 0.75);
    font: 650 0.7rem/1.3 var(--font-mono, monospace);
    text-transform: uppercase;
  }

  .story-footer__legal a { color: inherit; }

  .story-footer__legal a:hover {
    color: #000;
    text-decoration: underline;
  }

  @media (max-width: 980px) {
    .story-footer__grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  }

  @media (max-width: 720px) {
    .story-footer {
      padding-top: 2rem;
    }

    .story-footer__grid { grid-template-columns: 1fr; }

    .story-footer__bottom-card {
      align-items: flex-start;
      flex-direction: column;
    }

    .story-footer__brand-group {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.5rem;
    }

    .story-footer__social { width: 100%; }

    .story-footer__legal {
      gap: 0.6rem 0.8rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .story-footer *,
    .story-footer *::before,
    .story-footer *::after {
      transition-duration: 0.01ms !important;
    }
  }
</style>
