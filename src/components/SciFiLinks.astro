---
const groups = [
    {
        title: "Career & Recognition",
        links: [
            { label: "Work Experience", href: "/work" },
            { label: "Education", href: "/education" },
            { label: "Awards", href: "/awards" },
            { label: "Press Mentions", href: "/press" },
        ],
    },
    {
        title: "Portfolio & Research",
        links: [
            { label: "Photography Portfolio", href: "/photography" },
            { label: "Film Portfolio", href: "/films" },
            { label: "Research Projects", href: "/research" },
            { label: "Research Papers", href: "/research-papers" },
        ],
    },
    {
        title: "Thoughts & Connect",
        links: [
            { label: "Obsidian Vault", href: "/secret-lab" },
            { label: "Blog", href: "/blog" },
            { label: "Newsletter", href: "#newsletter" },
            { label: "Testimonials", href: "/testimonials" },
        ],
    },
];
---

<section class="scifi-links-section">
    {
        groups.map((group) => (
            <div class="group-container">
                {/* Optional: <h3 class="group-title">{group.title}</h3> */}
                <div class="scifi-grid">
                    {group.links.map((link) => (
                        <a href={link.href} class="scifi-card">
                            <span class="card-text">{link.label}</span>
                            <div class="card-decor" />
                        </a>
                    ))}
                </div>
            </div>
        ))
    }
</section>

<style>
    :root {
        --sci-fi-cyan: #00f3ff;
        --sci-fi-glow: rgba(0, 243, 255, 0.2);
    }

    .scifi-links-section {
        width: 100%;
        margin-bottom: var(--space-2xl);
        display: flex;
        flex-direction: column;
        gap: 3rem; /* Spacing between groups */
    }

    .group-container {
        width: 100%;
    }

    .scifi-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1.5rem;
        width: 100%;
    }

    .scifi-card {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.5rem 1rem;
        text-decoration: none;

        background: rgba(10, 15, 20, 0.4);
        border: 1px solid rgba(255, 255, 255, 0.4);
        border-radius: 8px;

        overflow: hidden;
        /* Slow transition for the 'off' state (fade out) */
        transition: all 1.5s ease-out;
    }

    .card-text {
        font-family: "Inconsolata", monospace;
        font-size: 0.95rem; /* Slightly larger */
        font-weight: 500; /* More prominent */
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: var(--text-primary); /* Brighter */
        z-index: 10;
        text-align: center;
        transition: color 1.5s ease-out;
    }

    /* Sci-Fi Hover Effects (Hover overrides pulse partially if needed, or coexists) */
    .scifi-card:hover {
        /* Fast transition for the 'on' state (activate) */
        transition: all 0.2s ease-out;

        border-color: var(--sci-fi-cyan, #00f3ff) !important;
        box-shadow: 0 0 15px rgba(0, 243, 255, 0.15) !important;
        background: rgba(0, 243, 255, 0.03) !important;
        transform: translateY(-2px);
    }

    .scifi-card:hover .card-text {
        transition: color 0.2s ease-out;
        color: #fff;
        text-shadow: 0 0 5px var(--sci-fi-cyan, #00f3ff);
    }

    /* Subtle corner markers */
    .scifi-card::before,
    .scifi-card::after {
        content: "";
        position: absolute;
        width: 6px;
        height: 6px;
        border: 1px solid transparent;
        transition: border-color 0.3s ease;
    }

    .scifi-card::before {
        top: 4px;
        left: 4px;
        border-top: 1px solid transparent;
        border-left: 1px solid transparent;
    }

    .scifi-card::after {
        bottom: 4px;
        right: 4px;
        border-bottom: 1px solid transparent;
        border-right: 1px solid transparent;
    }

    .scifi-card:hover::before {
        border-top-color: var(--sci-fi-cyan, #00f3ff);
        border-left-color: var(--sci-fi-cyan, #00f3ff);
    }

    .scifi-card:hover::after {
        border-bottom-color: var(--sci-fi-cyan, #00f3ff);
        border-right-color: var(--sci-fi-cyan, #00f3ff);
    }

    @media (max-width: 600px) {
        .scifi-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
