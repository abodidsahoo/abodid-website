---
import BaseLayout from "../layouts/BaseLayout.astro";
import {
    breadcrumbJsonLd,
    externalProfiles,
    personJsonLd,
    seoIdentity,
} from "../lib/seoData";

const pageTitle = "Contact Abodid Sahoo";
const description =
    "Contact Abodid Sahoo for paid consulting, Obsidian tutoring, workshops, photography, filmmaking, video editing, and creative technology projects.";
const superprofHref = externalProfiles.superprof;
const structuredData = [
    personJsonLd(),
    breadcrumbJsonLd([
        { name: "Home", path: "/" },
        { name: "Contact", path: "/contact" },
    ]),
    {
        "@context": "https://schema.org",
        "@type": "ContactPage",
        "@id": `${seoIdentity.siteUrl}/contact#contact`,
        url: `${seoIdentity.siteUrl}/contact`,
        name: pageTitle,
        description,
        mainEntity: {
            "@id": seoIdentity.personId,
        },
    },
];
---

<BaseLayout title={pageTitle} description={description} structuredData={structuredData}>
    <div class="contact-wrapper">
        <header class="contact-header">
            <p class="eyebrow">Work with me</p>
            <h1>Contact Abodid</h1>
            <p class="intro">
                Tell me about the consulting, tutoring, workshop, photography,
                filmmaking, or creative technology project you want to build.
            </p>
        </header>

        <section class="contact-content">
            <section class="intent-panel" aria-labelledby="contact-intent-title">
                <h2 id="contact-intent-title">I am available for</h2>
                <ul>
                    <li>Creative technology and AI-assisted research workflow consulting</li>
                    <li>Obsidian tutoring, second brain setup, and research system coaching</li>
                    <li>Workshops, talks, and mentorship for students or teams</li>
                    <li>Photography, filmmaking, video editing, and visual storytelling commissions</li>
                </ul>
                {
                    superprofHref ? (
                        <a href={superprofHref} target="_blank" rel="noopener noreferrer" class="platform-link">
                            View my Superprof tutoring profile
                        </a>
                    ) : (
                        <p class="platform-note">
                            Prefer booking tutoring through Superprof? Send me a
                            message and I will share the profile link.
                        </p>
                    )
                }
            </section>

            <section class="contact-form-section">
                <h2>Send A Message</h2>
                <form id="contact-form" class="contact-form">
                    <label for="contact-name">Name</label>
                    <input
                        id="contact-name"
                        name="name"
                        type="text"
                        autocomplete="name"
                        required
                        maxlength="120"
                        placeholder="Your name"
                    />

                    <label for="contact-email">Email ID</label>
                    <input
                        id="contact-email"
                        name="email"
                        type="email"
                        autocomplete="email"
                        required
                        maxlength="254"
                        placeholder="you@example.com"
                    />

                    <label for="contact-message">Your Message</label>
                    <textarea
                        id="contact-message"
                        name="message"
                        rows="6"
                        required
                        maxlength="5000"
                        placeholder="Tell me what you want to build, learn, document, edit, or organize."
                    ></textarea>

                    <button type="submit" class="send-button">Send</button>
                    <p
                        id="contact-form-status"
                        class="form-status"
                        aria-live="polite"
                    ></p>
                </form>
            </section>

            <div class="contact-grid">
                <!-- Email -->
                <div class="contact-card">
                    <div class="card-icon">
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="24"
                            height="24"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                        >
                            <path
                                d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"
                            ></path>
                            <polyline points="22,6 12,13 2,6"></polyline>
                        </svg>
                    </div>
                    <h3>Email</h3>
                    <a href="mailto:hello@abodid.com" class="contact-link"
                        >hello@abodid.com</a
                    >
                </div>

                <!-- Social Media -->
                <div class="contact-card">
                    <div class="card-icon">
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="24"
                            height="24"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                        >
                            <circle cx="12" cy="12" r="10"></circle>
                            <path d="M12 16v-4"></path>
                            <path d="M12 8h.01"></path>
                        </svg>
                    </div>
                    <h3>Social Media</h3>
                    <ul class="social-links">
                        <li>
                            <a
                                href="https://www.instagram.com/abodid.sahoo"
                                target="_blank"
                                rel="noopener noreferrer">Instagram</a>
                        </li>
                        <li>
                            <a
                                href="https://uk.linkedin.com/in/abodidsahoo"
                                target="_blank"
                                rel="noopener noreferrer">LinkedIn</a>
                        </li>
                        <li>
                            <a
                                href="https://twitter.com/abodidsahoo"
                                target="_blank"
                                rel="noopener noreferrer">Twitter / X</a>
                        </li>
                        <li>
                            <a
                                href="https://github.com/abodidsahoo"
                                target="_blank"
                                rel="noopener noreferrer">GitHub</a>
                        </li>
                        {
                            superprofHref && (
                                <li>
                                    <a
                                        href={superprofHref}
                                        target="_blank"
                                        rel="noopener noreferrer">Superprof</a>
                                </li>
                            )
                        }
                    </ul>
                </div>
            </div>

            <!-- Additional Info -->
            <div class="contact-note">
                <p>
                    Looking for my CV or portfolio? Visit the <a href="/about"
                        >About</a
                    > page for more information about my work and experience.
                </p>
            </div>
        </section>
    </div>

    <script is:inline>
        const contactForm = document.getElementById("contact-form");
        const statusNode = document.getElementById("contact-form-status");
        const trackGaEvent = (eventName, params = {}) => {
            try {
                if (typeof window.gtag === "function") {
                    window.gtag("event", eventName, params);
                }
            } catch (_error) {
                // no-op
            }
        };

        const getJourneyTracking = () => {
            const params = new URLSearchParams(window.location.search);
            return {
                sessionId:
                    window.__abodidAnalytics &&
                    typeof window.__abodidAnalytics.getSessionId === "function"
                        ? window.__abodidAnalytics.getSessionId()
                        : "",
                enquiryPath:
                    params.get("from") || params.get("source_page") || "",
                sourceName: params.get("source_name") || "",
                cta: params.get("cta") || "",
            };
        };

        if (contactForm && statusNode) {
            contactForm.addEventListener("submit", async (event) => {
                event.preventDefault();

                const submitButton = contactForm.querySelector(
                    "button[type='submit']"
                );
                const defaultButtonText = submitButton?.textContent || "Send";

                const formData = new FormData(contactForm);
                const payload = {
                    name: String(formData.get("name") || "").trim(),
                    email: String(formData.get("email") || "").trim(),
                    message: String(formData.get("message") || "").trim(),
                    tracking: getJourneyTracking(),
                };

                statusNode.textContent = "";
                statusNode.className = "form-status";

                if (submitButton) {
                    submitButton.disabled = true;
                    submitButton.textContent = "Sending...";
                }

                try {
                    if (
                        window.__abodidAnalytics &&
                        typeof window.__abodidAnalytics.prepareSubmission === "function"
                    ) {
                        await window.__abodidAnalytics.prepareSubmission();
                        payload.tracking = getJourneyTracking();
                    }
                    const response = await fetch("/api/contact", {
                        method: "POST",
                        headers: {
                            "Content-Type": "application/json",
                        },
                        body: JSON.stringify(payload),
                    });

                    const result = await response
                        .json()
                        .catch(() => ({ error: "Unable to send your message." }));

                    if (!response.ok) {
                        throw new Error(
                            result.error || "Unable to send your message."
                        );
                    }

                    contactForm.reset();
                    statusNode.textContent =
                        "Message sent successfully. I will get back to you soon.";
                    statusNode.classList.add("success");
                    trackGaEvent("generate_lead", {
                        event_category: "contact",
                        event_label: "contact-form-submit",
                        source_page:
                            payload.tracking?.enquiryPath ||
                            "unknown",
                        destination_page: window.location.pathname,
                    });
                } catch (error) {
                    const errorMessage =
                        error && typeof error === "object" && "message" in error
                            ? String(error.message)
                            : "Unable to send your message right now.";
                    statusNode.textContent = errorMessage;
                    statusNode.classList.add("error");
                } finally {
                    if (submitButton) {
                        submitButton.disabled = false;
                        submitButton.textContent = defaultButtonText;
                    }
                }
            });
        }
    </script>
</BaseLayout>

<style>
    .contact-wrapper {
        max-width: 900px;
        margin: 0 auto;
        padding: 0 24px;
    }

    .contact-header {
        text-align: center;
        padding-top: 4rem;
        margin-bottom: 4rem;
    }

    .eyebrow {
        font-family: var(--font-mono);
        font-size: 0.78rem;
        letter-spacing: 0.16em;
        text-transform: uppercase;
        color: var(--text-tertiary);
        margin: 0 0 1rem;
    }

    h1 {
        font-family: var(--font-display);
        font-size: 3.5rem;
        font-weight: 600;
        margin-bottom: 1rem;
        letter-spacing: -0.02em;
        color: var(--text-primary);
    }

    .intro {
        font-size: 1.25rem;
        color: var(--text-secondary);
        margin: 0;
    }

    .contact-content {
        padding-bottom: 6rem;
    }

    .intent-panel {
        border: 1px solid var(--border-subtle);
        background: var(--bg-surface);
        border-radius: 8px;
        padding: 1.5rem;
        margin-bottom: 3rem;
    }

    .intent-panel h2 {
        font-family: var(--font-display);
        font-size: 1.5rem;
        margin: 0 0 1rem;
        color: var(--text-primary);
    }

    .intent-panel ul {
        margin: 0;
        padding-left: 1.2rem;
        color: var(--text-secondary);
        line-height: 1.6;
    }

    .platform-link {
        display: inline-flex;
        margin-top: 1rem;
        color: var(--text-primary);
        font-weight: 700;
        text-underline-offset: 4px;
    }

    .platform-note {
        margin: 1rem 0 0;
        color: var(--text-tertiary);
        line-height: 1.5;
    }

    .contact-form-section {
        background: var(--bg-surface);
        border: 1px solid var(--border-subtle);
        border-radius: 8px;
        padding: 2rem;
        margin-bottom: 3rem;
    }

    .contact-form-section h2 {
        font-family: var(--font-display);
        font-size: 1.6rem;
        font-weight: 600;
        color: var(--text-primary);
        letter-spacing: -0.01em;
        margin: 0 0 1.25rem 0;
    }

    .contact-form {
        display: flex;
        flex-direction: column;
        gap: 0.9rem;
    }

    .contact-form label {
        font-size: 0.88rem;
        font-weight: 600;
        color: var(--text-secondary);
        letter-spacing: 0.02em;
    }

    .contact-form input,
    .contact-form textarea {
        width: 100%;
        font: inherit;
        color: var(--text-primary);
        background: var(--bg-color);
        border: 1px solid var(--border-subtle);
        border-radius: 10px;
        padding: 0.78rem 0.85rem;
        transition:
            border-color 0.2s ease,
            box-shadow 0.2s ease;
    }

    .contact-form textarea {
        resize: vertical;
        min-height: 140px;
        line-height: 1.55;
    }

    .contact-form input:focus,
    .contact-form textarea:focus {
        outline: none;
        border-color: var(--text-secondary);
        box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.08);
    }

    .send-button {
        align-self: flex-start;
        margin-top: 0.25rem;
        border: 1px solid var(--border-subtle);
        border-radius: 999px;
        background: transparent;
        color: var(--text-primary);
        font-family: var(--font-ui);
        font-size: 0.92rem;
        font-weight: 600;
        padding: 0.6rem 1.1rem;
        cursor: pointer;
        transition:
            background-color 0.2s ease,
            border-color 0.2s ease,
            color 0.2s ease;
    }

    .send-button:hover:not(:disabled) {
        background: var(--bg-surface-hover);
        border-color: var(--text-primary);
    }

    .send-button:disabled {
        opacity: 0.6;
        cursor: not-allowed;
    }

    .form-status {
        min-height: 1.3rem;
        margin: 0.25rem 0 0;
        font-size: 0.92rem;
        line-height: 1.4;
        color: var(--text-secondary);
    }

    .form-status.success {
        color: #49c673;
    }

    .form-status.error {
        color: #ff7f7f;
    }

    .contact-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-bottom: 4rem;
    }

    .contact-card {
        background: var(--bg-surface);
        border: 1px solid var(--border-subtle);
        border-radius: 12px;
        padding: 2.5rem;
        transition: all 0.3s ease;
    }

    .contact-card:hover {
        transform: translateY(-4px);
        border-color: var(--text-secondary);
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
    }

    .card-icon {
        width: 48px;
        height: 48px;
        background: var(--bg-color);
        border: 1px solid var(--border-subtle);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
        color: var(--text-primary);
    }

    .contact-card h3 {
        font-family: var(--font-sans);
        font-size: 1rem;
        font-weight: 600;
        color: var(--text-tertiary);
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin: 0 0 1rem 0;
    }

    .contact-link {
        display: inline-block;
        font-size: 1.2rem;
        color: var(--text-primary);
        text-decoration: none;
        border-bottom: 2px solid var(--border-subtle);
        padding-bottom: 4px;
        transition: all 0.2s ease;
    }

    .contact-link:hover {
        border-bottom-color: var(--text-primary);
    }

    .social-links {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .social-links a {
        font-size: 1.1rem;
        color: var(--text-primary);
        text-decoration: none;
        border-bottom: 1px solid transparent;
        padding-bottom: 2px;
        transition: all 0.2s ease;
        display: inline-block;
    }

    .social-links a:hover {
        border-bottom-color: var(--text-primary);
    }

    .contact-note {
        text-align: center;
        padding: 2rem;
        background: var(--bg-surface);
        border-radius: 8px;
        border: 1px solid var(--border-subtle);
    }

    .contact-note p {
        margin: 0;
        font-size: 1rem;
        color: var(--text-secondary);
        line-height: 1.6;
    }

    .contact-note a {
        color: var(--text-primary);
        text-decoration: underline;
        text-underline-offset: 2px;
    }

    .contact-note a:hover {
        text-decoration-thickness: 2px;
    }

    @media (max-width: 768px) {
        h1 {
            font-size: 2.5rem;
        }

        .contact-grid {
            grid-template-columns: 1fr;
        }

        .contact-form-section {
            padding: 1.5rem;
        }

        .contact-card {
            padding: 2rem;
        }
    }
</style>
